# Import one meeting twice

Time: 8 minutes. Complete dependency and login setup before starting the timer.

## Do
Open Connections → Import record. Choose Northstar. Replace the prefilled JSON with 01-planning/sample-data/meeting.json. Import it; open Northstar → Meetings and find demo-transcript-001. Record the count. Repeat with the exact same JSON and client.

## Leave with
The source appears once and the second import leaves the count unchanged.

## Check
The browser and CLI have different practice identities. Keep both imports in the same browser.

Record the actual result and any unfinished step. The completed reference is a fallback, not evidence that your new copy passed.
