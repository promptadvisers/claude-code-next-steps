# Package it for a teammate

Time: 6 minutes. Complete dependency and login setup before starting the timer.

## Do
Write prerequisites, startup, one command example, known limitations and an owner. Review the actual changed files. Exclude credentials, local data and generated folders. Record a local reviewed version. A real remote release can happen after its destination and contents are reviewed.

## Leave with
A local handoff a teammate can follow.

## Check
Do not invent a GitHub URL, tag or successful teammate run.

Record the actual result and any unfinished step. The completed reference is a fallback, not evidence that your new copy passed.
